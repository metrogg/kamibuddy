# 2026-09-16 工作日志

（承接 2026-09-15.md —— 那天的任务诊断面板第一批 ①③ 在此继续。）

## 任务诊断面板第二批：② 上下文占用 + ④ 单步详情 + ③ 按步归组

四区结构至此齐了（① 会话总览 / ② 上下文 / ③ 轮·步台账 / ④ 单步详情）。

### 三处「唯一处」抽取（都是本次改动的直接后果，不是顺手重构）

1. **`renderer/context-usage.tsx` 加导出 `ContextUsageBreakdown` + `contextUsagePercent`**。
   圆环浮层与面板 ② 是并列展示位，共用同一份「占比读数 + 分类条 + 图例 +
   `~` 前缀 + 估算尾注」。**标注是口径的一部分，不是排版细节** —— 抄一份到面板
   就等于埋一个漂移点。圆环那侧现在只剩外壳（定位 / 标题 / 关闭）。
2. **新建 `renderer/snapshot-breakdown.tsx`**，把诊断页私有的 `SnapshotBreakdown`
   （+ `SEG_PALETTE`）整体搬出，两处 import。搬的是组件，保住的是措辞契约：
   「系统提示词分段 = **真实计数**（不带 ~）」「消息组成 = 条数 / **字符数** / 字符占比」。
3. **`renderer/run-timeline.ts` 加 `foldRunSteps` + `LedgerStepItem` 类型**。
   台账两级 fold：`foldRunLedger` 出「轮」，`foldRunSteps` 再切成「步」。

### 工具 → 步的归属只能按位置推（关键设计约束）

台账里 `tool_call` **只带自己的 `toolCallId`，不带「属于哪一轮模型调用」**。
pi 的工具执行总在某次模型调用之后、下一次之前，所以「前一个 llm_call」是唯一可推的
归属 —— 与 `foldRunLedger` 判 run 归属的手法同源（runId 跨 daemon 代际会撞名，只能看位置）。
重试 / 压缩跟着「当前步」走（它们的时刻本就在该步之后、下一步之前，挪到轮级会打乱顺序）。
首个 llm 之前的条目（台账截尾后可能出现）落进 `call: undefined` 的前导组，**不编造归属**。

`LedgerStepItem = Exclude<LedgerItem, { kind: "llm" }>` 把「rest 里结构上不可能有 llm」
写进类型，消费方不用在渲染时再判一次。**先写成 `readonly LedgerItem[]` 会 tsc 报错**
（RestRow 收窄不了）—— 那正是把保证交给类型的理由。

### 两个界面决定（都写进代码注释了，防后人「顺手改回」）

- **④ 内联展开，不学 dsh 另开 inspector 面板**：右侧栏本就是窄栏（默认 440），
  再分一层双栏两边都不够用；内联还能让答案紧贴被点的那行。
- **展开块排在「该步的工具行之前」**：它是「这一步花了多少、看到了什么」的答案，
  直接接在被点的那行下面；工具行随后补「它做了什么」。
- 展开键 = `runId#startedAt#turnIndex`。**单用 runId 会让新老两代的同一「run-1」
  一起展开**（跨 daemon 重启撞名），带毫秒级 startedAt 才唯一。
- `reasoning` 标注「是 output 的子集、已包含在输出里」，**且不加 `~`**
  （`~` 在本项目专指估算，reasoning 是 provider 上报的真值）。

### 样式

`.task-diag-heading` 改 flex（标题带限定语：「实时」「点开某一步看该轮详情」，
那是口径的一部分）；新增 `.task-diag-step-group` / `.task-diag-step-btn` /
`.task-diag-caret` / `.task-diag-detail`。
**缩进算式写在 `.task-diag-step-btn` 一处**（run 8 + margin 4 + padding 4 = 16，
与改造前同位），它的 `padding` 简写有意覆盖 `.task-diag-step` 的 `padding-left`。
步行按钮化后：全局 `button` 规则已给足默认重置（font inherit / 无底无框 / padding 0），
**只补 width / text-align / 缩进三条**，不重写一遍；`:active` 按项目自己的分类
（整行可点区域 → 底色加深不缩放）加进了文件末尾那块列表行清单。

### 验证

`tsc --noEmit` ✓ ｜ `check:deps` ✓（282 文件）｜ `check:tokens` ✓（真违例 0）｜
`run-timeline.test.ts` 16 用例 ✓（新增 5 个）｜ 全量 **104 文件 / 1908 用例**：
1 失败 —— `src/sandbox/confinement.win.test.ts > 超时杀掉整棵进程树`，
**并发下的计时偶发**（Job Object 杀孙进程要 5s），单跑该文件 10/10 通过。
与本次改动无关（改的全在 renderer / CSS）。

### 清单同步（`docs/可观测性清单.md`）

- 新增 **VIEW15**（任务级诊断面板），计数 83 → **84**（✅53 ／🟡10 ／❌17 ／⛔3 ／❓1）。
- **`reasoning` 移出「已采集但没展示」表** —— 它现在是 ④ 的一部分，那条记载已成假事实。
- 优先级建议里的「CACHE5 + ~~reasoning~~ + costBreakdown 分项」同步划掉 reasoning。

### 未做（第三批）

② 已做完，剩下的是**统计页接收 3 块 + 诊断页瘦身为环境自检**（顺手补 OPS1「观测健康」，
正好填掉那个 ❌）。诊断页瘦身后，它的 `LedgerRunView` / `LlmDetail` / `ITEM_BOUNDS` 那套
私有实现可以整块删掉（面板已覆盖），`END_REASON_LABELS` 两处副本也可合并到面板。

## 左侧栏行内操作：四图标 → 一个「⋯」菜单（对齐 WorkBuddy）

用户报「左侧边栏的图标也有重叠了」，附了两张图。**先说一个重要的核对结论**：
那两张图是 **WorkBuddy 本身**，不是我们的应用 —— 判据是四个我们仓库里根本不存在的串：
`发现应用`、`Buddy加油站`、`5.5.6`、品牌名 `WorkBuddy`（我们是 `嘉立创Work` / `V0.1.0`，
`src/renderer/index.html` 的 title 也是它）。**下次再遇到这种事，先跑这四条串的 grep 定性，
别靠肉眼看品牌字**（我这轮前前后后辨认了好几遍）。

**真凶**（从我们应用最近的截图 + 代码双向确认）：任务行 hover 浮出的是
**四个 20px 图标按钮**（打开文件夹 / 导出 / 重命名 / 删除），`gap: 2px` ——
216px 窄栏里 glyph 之间只剩 2px，看着就是"叠在一起"，而且整簇压在右侧时间戳那一格上。
WorkBuddy 的做法在用户第二张图里：**行尾只留一个「⋯」，动作全收进菜单**
（它的菜单：打开文件夹 / 重命名 / 保存到工作空间 / 分享任务 / 删除任务）。

改法（`renderer/sidebar.tsx` + `index.css`）：

- `.task-item-ops` 里只留一个 `⋯`（`IconMore` 15px，`aria-expanded`）；
- 新增 `menuPath` 态（与 `menuCwd` 同款"记 path、同时至多一个"），
  菜单卡片复用 `.pop-menu.space-menu` + `.space-menu-item`（含 `.space-menu-danger` 的删除红），
  只加一条 `.task-op-menu` 锚点（`top: 100%` / `right: 6px`，对齐触发钮右缘）；
- Esc 的 effect 从只关 `menuCwd` 扩成两个菜单一起关（同 model-menu 的既有写法）；
- `IconEdit` / `IconExport` / `IconTrash` 三个 import 随之删除（否则 TS 报未使用）；
- 菜单条目**不带图标**：本项目所有菜单（model / permission / plus / space）都是纯文字 + 右侧对勾，
  只有 WorkBuddy 那套带前置图标 —— 这里跟自家语言一致，不跟它。

**顺带修一个我上轮引入的真回归**：`.chat-header` 的让位内边距我上次写成了简写
`padding: var(--space-3) 84px`，**顺手把左侧也从 48 抬到 84**，返回按钮凭空右移 36px。
已改成四值分写 `var(--space-3) 84px var(--space-3) 48px`，并把"两侧必须分开写"的理由钉进注释
（左侧只有一个侧栏开关，不该跟着右侧涨）。**教训**：给一条边算让位时别用简写。

**没动**（同区但另一回事，已告知用户）：空间分组行是 `📁 ⌄ 名称`，folder 与 chevron
两个图标只隔 `--space-1`(4px)，而 `.nav-item` 的图标↔文字是 `--space-3`(8px)。
WorkBuddy 每行只有一个图标 —— 要不要也收成一个，等用户定。

验证：`tsc --noEmit` ✓ ｜ `check:deps` ✓（282 文件）｜ `check:tokens` ✓（真违例 0）。
**真机验证仍未做**（纯 UI 改动，四道静态校验都看不出布局）。

## 插入任务（steer）：功能早就接好了，但被会话互斥链吞掉（真架构 bug）

**用户报「agent 执行时再输入一条命令，界面没反应」**。排查结论分三层：

1. **pi 原生就有**，而且我们**早已接入**：`AgentSession.steer/followUp/clearQueue`、
   `PromptOptions.streamingBehavior`、`queue_update` → 我们的 `queue_changed`（带队列内容）。
   `session-host.prompt` 在 isStreaming 时默认 steer（`core/session-host.ts`），
   chat-view 的 placeholder 也写着「补充说明会插入当前任务…」。
2. **真凶在 daemon 的互斥链**：`[INVOKE.prompt]` 把每条消息 `enqueue(bucket, …)`，
   而链上前一个 prompt **await 的是整段 run** —— 第二条消息排在整轮后面，
   出链时流式已结束，steer 分支永远走不到，退化成「跑完后新一轮」。
   `whileStreaming` 参数实际是死代码。**修法**：`bucket.running` 时旁路直调
   `host.prompt(...)`（steer 是信号不是写操作，与 abort 同一条豁免理由）。
3. **反馈太弱**：发出去只有流末尾一行灰色「N 条消息排队中」。WorkBuddy 是
   **输入卡上方的排队 chip**（文本 + 编辑 + 删除）。

**配套改动**：

- `session-host`：①带 `whileStreaming` 的 prompt 撞上「run 恰好收尾」的竞态时，
  走 `AgentSession.prompt` 的 `streamingBehavior` 选项（消息不丢、不裸调被拒）；
  ②新增 `rewriteQueue(steering, followUp)` = `clearQueue()` + 按序重入队 ——
  **pi 只有整队清空、没有按条删除**，按条语义是这么合成的。两个代价（注释已钉）：
  clearQueue 只回文字 → 带图排队被删/编过一次后变纯文本；清空→重入队之间有
  无队列窗口，若 run 恰在此间收尾，消息挂队列等下一轮（不丢、chips 仍在）。
- 通道：`session:queue-rewrite`（ipc / bridge / preload 三层）。
- `shared/conversation.ts`：`queueCount`（纯计数）→ `queued`（内容数组，
  `QueuedMessages` 类型进 session-events），新增纯函数 `removeQueuedMessage`
  （摘**第一条**匹配、steering 优先、找不到原样返回供调用方跳过重排）+ 5 个用例。
- `chat-view`：计数行删除，输入卡上方渲染排队 chips（「待发送」徽标 + 文本 +
  编辑＝填回草稿并摘除 / 删除＝摘除，都走 rewrite）；`App.rewriteQueue` 接线。

**排查手法记一笔**：这轮最值钱的一步是「顺着 IPC 往下挖到互斥链」——
UI 层看是「没反应」，实际是消息被静默排到了几分钟后。判断「某条链路是否生效」，
要沿 renderer → ipc → daemon handler → session-host 全程走一遍，
**任何一环的排队/串行机制都可能让后面的分支变成死代码**。

## 语义反转（用户定，2026-09-16 上午）：缺省 = 排队，steer 才配按钮

初版把缺省做成了 steer（补一句就插进当前这轮），用户纠正：**输入进去默认是
排队等上一个任务做完，「立即插入」按钮才是立刻插进这一轮**。

- **缺省值唯一处**：daemon `[INVOKE.prompt]` 的旁路分支里 `whileStreaming ?? "followUp"`
  （写在这里而不是 session-host，让「哪条路是默认」只有一个地方说了算）；
  session-host 反转为 `whileStreaming === "steer" ? steer : followUp`，并补了
  一条钉住缺省的测试（流式缺省 = followUp，steer 零调用）。
- **两条路在视觉上分开**：composer 流式期间 = 停止键 + 「立即插入」文字按钮
  （`.insert-btn`，次要色——例外不配主按钮色；disabled 按 §3.1 opacity .4）。
  回车/非流式发送 = 排队。「立即插入」disabled 条件与发送键同（空文本/超限）。
  **教训**：给 `submit` 加参数后，`onClick={submit}` 会把 MouseEvent 当第一参传进去
  （TS 抓到），改成 `onClick={() => submit()}`。
- chips 跟着语义细化：`queuedItems` 带 `kind`（steering/followUp），
  徽标从「待发送」改为「将插入 / 排队中」，followUp 项多一个「立即插入」钮
  （把那条挪进 steering 队尾）。纯函数 `insertQueuedNow`（已在 steering / 找不到
  原样返回）+ 4 个用例；与 `removeQueuedMessage` 共用模块级 `dropFirstMatch`。


