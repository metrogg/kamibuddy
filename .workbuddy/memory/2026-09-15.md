# 2026-09-15

## 调研：WorkBuddy 的「代码办公」与「日常开发」分界

结论（证据来自 `docs/workbuddy分析/02-prompts.md` §4.2 + §4.1）：

- **场景页签是空的区分。** `welcomemode` 下 code 与 work 的 `prompt.tpl`
  **逐字节相同**；工具白名单由 `workMode`（ask/craft/plan/expert）决定，
  不由 welcomeMode 决定。唯一历史差异是 CodeBuddy 时代的
  `workbuddy-craft-code-prompt.tpl` 多一条「产物回传小程序」条款，重构后已消失。
- **真正的分界线在「运行环境」这一层**：WorkBuddy 桌面（办公）vs
  CodeBuddy CLI/IDE（开发）共用同一个 CLI 内核，靠工具白名单 + 权限基线裁开。
  桌面端刻意不要：LSP、worktree、Notebook、checkpoint 回滚、Agent Teams、CI 集成、
  `/goal` 自治循环 —— 这些是 CLI 的产品位。
- 桌面端反而特有：present_files 双路交付、内联可视化（read_me/show_widget）、
  sites 发布公网链接、本地 Office 引擎。

## 我们的位置（代码盘点，非勾选状态）

分岔逻辑与 WorkBuddy 同构：`sceneId` 只进提示词骨架，
工具面由 `mode.tools` 决定（craft 白名单 21 个）。

差距按严重度排（都在「让 agent 真的把代码跑起来」这条链上）：

1. **powershell 无后台执行** —— `MAX_TIMEOUT_SECONDS=600`，跑 build/test/dev server
   会阻塞到超时。WorkBuddy 的 Bash 有 `run_in_background` + TaskOutput 取结果。
2. **工具输出不外置** —— 只有 read_document / 子代理 24k 截断；通用工具输出
   直接进上下文。跑 `npm install` 就爆。
3. 无 sites/artifact 发布、无会话回滚、无 worktree、无 LSP、无 Teams。
4. 已对齐：交付链路、文件搜索、联网、定时、内联可视化、代码渲染侧
   （代码块/文件树/diff 统计/代码预览面板）。

⚠️ 本轮只做调研，未改任何代码与文档。

## 修正（同日，用户截图 + 源码验证）

**上面「场景页签是空的区分」只对了一半。提示词层确实相同，但界面层不同。**

证据：`docs/WorkBuddy-reference/extracted/renderer/assets/home-DrgzoIb-.js`
（源文件 `packages/agent-ui/src/modules/home/components/use-home-input-providers.tsx`）

输入区功能芯片的三场景差异，全貌就这三条：

```js
if (desktopCloudTaskEntryEnabled)  → home-task-target  (FooterStart)
if (sceneMode === "design")        → home-design-options  (AfterAddMenu)
if (sceneMode === "code" && cwd && taskTarget !== "cloud")
                                   → home-worktree  (FooterAfterWorkspace)
                                   WbWorktreeChip{cwd, onSelectBranch}
```

`work` 场景**没有**任何专属芯片。所以「代码开发多出一个分支选项」= worktree 芯片，
用户截图属实。

另：`mapHomeWelcomeMode(mode)` 返回的是 `"coding"` / `"working"` / `"design"`
—— 注意是 `-ing` 后缀，即 CodeBuddy 遗留命名，不是新插件目录名 `code`/`work`/`design`。
（02-prompts.md §1.1 记过这条命名规律，此处得到运行时印证。）

## Worktree 任务隔离机制（`main/server.js` 的 WorktreeTaskDomain）

- **目录**：`~/WorkBuddy/Worktrees/<原仓库目录名>/<branchSlug>-<uid8>`
- **分支**：`workbuddy/<branchSlug>-<uid8>`
  - `slugifyBranch`：小写化 + 非 `[a-z0-9]` 全转 `-` + 去首尾 `-`
  - `uid` = `randomUUID().replace(/-/g,'').slice(0,8)`
- **RPC 9 通道**（`WORKTREE_TASK_RPC_CHANNELS`）：IS_GIT_REPO / LIST_LOCAL_BRANCHES /
  FETCH_BRANCHES / COUNT_WORKTREES / CLEANUP_WORKTREES / GET_WORKTREE_STATUS /
  CREATE_WORKTREE / DELETE_WORKTREE / CHECKOUT_BRANCH
- **createWorktree 失败回滚**：`removeWorktree` → `rm -rf worktreePath` → throw
- **checkoutBranch 的 dirtyPolicy 四态**：`abort`(默认) / `commit` / `stash` / `discard`
- **cleanupWorktrees**：跳过 `hasUncommittedChanges`、跳过近期（olderThanDays），
  只删 `workbuddy/` 前缀分支；返回五项计数器
- **阈值常量**：`WORKTREE_COUNT_WARNING_LIMIT = 25`、`WORKTREE_COUNT_CLEANUP_LIMIT = 50`
- **参数校验**：repoCwd / worktreePath 必须绝对路径，否则 throw
- **组件**（`packages/agent-ui/src/modules/worktree/worktree-chip.tsx`）：
  依赖 `useWorktreeGit`（isGit/branches/currentBranch/worktreeCount）、
  branch-list、cleanup-dialog、overlays、source-map

桌面端串联（`useCreateHomeTask`）：
1. 创建任务前若 `worktreeBaseBranch && cwd` → `createWorktree({repoCwd, baseBranch})`
2. 成功 → `cwd = worktree.worktreePath` + `recordWorktreeSource(path, sourceCwd, taskBranch)`
3. 失败 → toast `worktree.createFailedFallback`（warning），**降级用原目录继续**
4. 发消息前过 `useHomeBranchSwitchGate`：脏工作区弹 Modal 三选一（`BranchSwitchDirtyPicker`）

## 我们的差距

`src/` 里 **worktree 零命中**。无分支选择、无隔离副本、无清理。
对齐清单 C22 标 ❌ 属实，但「WorkBuddy 也没有」的印象是错的 —— 桌面端有，
且是 code 场景的专属基础设施。

## 方法论沉淀

逆向探针流程已存为 skill：`~/.workbuddy/skills/workbuddy-reverse-probe/`
（Grep 只取路径定位文件 → probe.mjs 用 indexOf+slice 提取上下文 → 回写对齐清单）。
本轮全程约 6 次工具调用；若用内容 grep 会在第一步就被长行吞掉。

## 实施：worktree 任务隔离（对齐清单 C22 / L27）

**先补清单**（`docs/workbuddy对齐清单.md`）：C22 改写为桌面端真实机制、F3 补
「场景差异不止在提示词」、新增 L27「场景专属输入区芯片」、计数 194 → 195。

**新增/改动 11 个文件**（按依赖方向从下往上）：

| 层 | 文件 | 内容 |
|---|---|---|
| shared | `worktree.ts` + `.test.ts` | 类型 + 纯函数（slugifyBranch / generateWorktreeUid / repoDirName / requireRepoCwd / requireBranchName / isAbsolutePath）；34 测试 |
| core | `worktree.ts` + `.test.ts` | git CLI 操作（isGitRepo / resolveRepoRoot / getBranchList / getRepoStatus / createWorktree / removeWorktree / listLinkedWorktrees / cleanupWorktrees / worktreeInfoFromCwd / isWorktreePath）；22 测试真跑 git |
| shared | `ipc.ts` / `bridge.ts` | 通道 worktreeBranches + setWorktreeBranch；WorkspaceSnapshot 加 worktreeBranch |
| shared | `session-events.ts` | SessionState 加可选 worktree 字段 |
| preload | `index.ts` | 两通道暴露 |
| daemon | `session-registry.ts` | 桶加 pendingWorktreeBranch（消费一次）+ worktree |
| daemon | `workspace-model.ts` | isTaskPrivateCwd 认副本路径（归任务区 + 不让转正 rename 副本） |
| daemon | `index.ts` | 模块级意图变量 + 2 个 handler + createHost 建副本切 cwd + resume 反推 + emitSessionEvent 注入 |
| core | `workspace.ts` | validateWorkspacePath 放行副本路径（否则副本会话 resume 必失败） |
| renderer | `worktree-chip.tsx`（新）+ `home-view.tsx` + `icons.tsx` | 芯片 + 接入 .context-row + IconBranch |

## 三个真踩到的坑（都有代价，别重犯）

1. **`validateWorkspacePath` 会拒掉副本路径** —— 副本在 `<配置目录>/worktrees/` 下，
   而该函数拒绝配置目录内的一切路径。不修的话副本会话 **resume 直接报
   「会话的工作目录不可用」**，功能等于不可用。修法：在配置目录那条规则里加
   `isWorktreePath(path)` 例外（只开副本根之内，配置目录本身照旧拒）。
2. **git 在 Windows 回报的 worktree 路径是正斜杠**（`C:/Users/...`），而
   `path.join` 给的是反斜杠。不 normalize 的话 cleanedPaths 与 WorktreeInfo.worktreePath
   「看起来相等却不等」—— 清理删对了文件，但所有路径比对静默失配。测试抓到的。
3. **本机 git 单次调用约 1–2 秒**（Windows + 杀软）：22 个用例跑了 180 秒，
   默认 5s 的 testTimeout 稳定误报。修法：文件级 `vi.setConfig({testTimeout: 60_000})`
   + `rm(..., {maxRetries: 10, retryDelay: 200})`（Windows 上刚跑完的 git 交句柄有延迟，
   直接 rm 撞 EBUSY）。

## 设计取舍（写下来免得后人改回去）

- **意图存 daemon 不存 renderer**：WorkBuddy 把它放在 home 组件 state 里，
  但那样切页面重建组件后，界面说「未启用」而新任务照样建副本。我们的
  `pendingWorktreeBranch` 是 daemon 模块级变量，并随 `workspaceSnapshot` 下发
  （与 current 同源，都是「下一个新任务怎么起」）。
- **新建任务不重置它**（与 defaultWorkspaceDir 相反）：空间选择在新建任务时清空是
  产品语义，副本开关是「我习惯在隔离副本里干活」的偏好，该沿用。
- **副本路径归任务区**：既避免以 `main-a1b2c3d4` 这种目录名进空间区成组，
  更要紧的是让 `isOwnedSessionDir` 为假 —— 「保存到工作空间」走新建目录的回退分支，
  **不会 rename 副本目录**（rename 会让 `.git/worktrees/` 指向不存在的路径，仓库半坏）。
- **v1 未做**：脏工作区切换门（`BranchSwitchDirtyPicker` 四态）、副本计数与清理界面入口
  （`cleanupWorktrees` 已实现但无 UI 调用点）、resume 后的 baseBranch/sourceCwd 还原
  （反推不出，按类型缺省）。

## 验证结果

- `tsc --noEmit` ✓
- `check:deps` ✓（263 文件，依赖方向无违规）
- `check:tokens` ✓（真违例 0 处）
- 全量测试 **97 文件 / 1773 用例全绿**（新增 56 个：shared 34 + core 22）
- **未做真机验证**：芯片的实际显示与点击要走 `npm run dev` 手工点，
  本轮只到「类型 + 单测 + 静态校验」这一层。下次接手时优先手工验证：
  切到代码场景、选一个 git 仓库目录、芯片应出现、选分支后发消息应看到
  cwd 变成 `<配置目录>/worktrees/...`。

## 用户实测反馈：「切到代码开发什么都没变化」

**根因（查会话文件得到实证，不是猜）**：`~/.kamibuddy/sessions/` 最近几个会话的
cwd 都是 `C:\Users\wzd\KamiBuddy\2026-09-15-11-54-39` 这种**自动分配的时间戳目录**
（临时任务），不是 git 仓库。而原实现的门控是
`sceneId === "code" && cwd 非空` + 组件内 `isGitRepo` 判定 → **整块不渲染**，
所以切场景零视觉变化。

**教训（比 bug 本身重要）**：严格照抄 WorkBuddy 的「不可用就不渲染」在**我们的
默认状态下会命中盲区** —— 我们新建任务默认是临时任务（cwd 待分配/自动目录），
与 WorkBuddy 一样，但 WorkBuddy 用户多数会先选空间，于是它的隐藏策略不暴露问题，
我们暴露了。「功能存在但用户发现不了」等价于功能不存在。

**修法（最终采纳：对齐 WorkBuddy，不自行发挥）**：
先做了一版「始终渲染 + 不可用时在弹层说明原因」的有意偏离（`home-view` 只留场景
门控、组件三态、新增 `.ws-note`）。**用户随即要求回到与 WorkBuddy 一致的隐藏策略**，
已回退：

- `worktree-chip.tsx`：非 git 仓库 / 还没查完 → 返回 null（整块不渲染）；
- `home-view.tsx` 门控：`sceneId === "code" && cwd 非空`
  （对齐 WorkBuddy 的 `sceneMode === "code" && cwd`）；
- `index.css`：`.ws-note` 已删干净。

保留了一处与可见性无关的正确性改进：换目录时先 `setRepo(undefined)` 复位，
否则会短暂显示上一个目录的分支列表，用户可能按着旧列表选一个并不存在的分支。
（另：v1 那个 `cancelled` 守卫本来就有。）

**记一条判断依据**：可见性/交互策略一律**对齐 WorkBuddy，不自行发挥** ——
用户要的是复刻，不是改进。有偏离想法时先问，别直接做进代码。

## 字体调研（同日）

**我们**（`src/styles/tokens.css` 唯一真源）：

```css
--font-body: "PingFang SC", "MiSans", -apple-system, BlinkMacSystemFont, "Segoe UI", "Microsoft YaHei", sans-serif;
--font-mono: ui-monospace, "Cascadia Code", Consolas, monospace;
```

MiSans **随包**（@font-face 在 `src/renderer/index.css`）。tokens.css 注释里的设计意图：
macOS 命中苹方；Windows 上苹方 / -apple-system / BlinkMacSystemFont 全不在 → 落 MiSans
（有真 400/600 字重，避免雅黑合成加粗发虚）；Segoe UI 与微软雅黑兜 MiSans 子集未覆盖的生僻字。

**WorkBuddy**：

- 随包字体 **Assistant**，4 字重（Regular / Medium / SemiBold / Bold）——
  `renderer/assets/Assistant-*-*.woff2`（证据：`docs/WorkBuddy-reference/asar-files.txt`）。
- Assistant 是 **Google Fonts 的希伯来-拉丁字体**（Adobe + Ben Nathan，OFL），
  语言支持只有 `hebrew / latin / latin-ext` —— **不含中文**，所以它的中文完全交给系统字体。
- 组件级普遍写 `font-family: var(--wb-font-family, "PingFang SC")`；
  **`--wb-font-family` 在所有已解包 CSS 里找不到定义处**（只有 var() 使用）。
- 等宽 `--wb-font-mono`；代码块 / 文件树 / markdown 编辑器用
  `--vscode-editor-font-family` / `--vscode-font-family`；登录页另用 Montserrat（营销页）；
  KaTeX 一整套（数学公式）。

**关键对比**：**随包方向相反** —— 我们随包**中文**字体（MiSans，解决 Windows 中文发虚），
它随包**拉丁**字体（Assistant，统一拉丁字形）。另外我们是**单点真源**
（DESIGN.md §2.6 强制，还专门修过「MiSans 声称已接实际没接」的账目错误），
它是**散装的**（变量只有使用没有定义，各组件各写 fallback）。

**未取到**：`renderer/assets/index-BwrIESZz.css`（主入口 CSS，全局 @font-face 应在里面）
未被解包，从 app.asar 提取失败两次（详因见 skill `workbuddy-reverse-probe` 新增的附录）。

## 字体改回系统字体（同日，用户反馈）

用户反馈「MiSans 在一些电脑上发虚不清」，要求改回微软雅黑。已改（`src/styles/tokens.css`）：

```css
--font-body: "PingFang SC", -apple-system, BlinkMacSystemFont, "Segoe UI", "Microsoft YaHei", "MiSans", sans-serif;
```

MiSans 从第 2 位挪到**栈尾** → Windows 回到「拉丁 Segoe UI + 中文雅黑」。
同步更新了 `src/renderer/index.css` 的 @font-face 注释与 `resources/fonts/README.md`
（新增「当前状态」一节，保留「当初为什么随包」作为记录）。

**根因（值得记）**：MiSans 子集只有 **3876 字**（ASCII + GB2312 一级汉字），
生僻字**本来就落回雅黑** → 同一段落里 MiSans 与雅黑**混排**，字形粗细不匀；
加上它是随包 webfont，在 Windows 上缺系统字体那种 hinting，部分 DPI/缩放下发虚。

**取舍**：改回后 `font-weight: 600` 由雅黑**合成加粗**（雅黑无真 600 字重）——
这正是当初随包 MiSans 的唯一理由（见 resources/fonts/README.md）。用户选择清晰度优先。

**踩过的差点犯的错**：第一版我把 `"Microsoft YaHei"` 放在栈第 2 位，那会让 **Windows 上
的拉丁也用雅黑**（雅黑含拉丁字形，且排在 Segoe UI 之前）。原栈的意图是「拉丁 Segoe UI /
中文雅黑」，只是 MiSans 插在第 2 位把两者都盖住了 —— 所以正确改法是**把 MiSans 挪到末尾、
其余顺序不动**，不是把雅黑提前。

**未做（待用户定）**：MiSans 的 @font-face 与两个 woff2（共 951 KB）仍随包，现只作栈尾兜底、
几乎不命中。要不要清掉需单独决策。

**顺带发现（未动）**：`src/renderer/index.css` 里有 **19 处**等宽字体栈字面值
（`ui-monospace, "Cascadia Code", Consolas, monospace`），与 `--font-mono` 同值却没引用变量
—— 违反 DESIGN.md §2.6「不得就地写 font-family 字面值」，属既存技术债。

**排查手法记一笔**：判断「某功能为何不显示」时，**先读会话文件 header 的 cwd**
（`~/.kamibuddy/sessions/*.jsonl` 第一行），比问用户「你选目录了吗」快且准。
注意 `~/.kamibuddy` 是隐藏目录，Glob 工具默认不匹配它 —— 要写 node 脚本读。

## 评估：诊断页与统计页（调研，未改任何代码）

### bug1（最硬）：`tool_stream_progress` 被三处当成 `tool_progress` 处理

`tool_progress`（工具 stdout，字段 `delta`）与 `tool_stream_progress`
（参数生成期进度，字段 `rawArgs`）是 `session-events.ts:339/350` 里**两个并列类型**。
本机实测 09-15 单日：`tool_progress` **0 条**，`tool_stream_progress` **30,944 条**。
三处名单都写了前者、漏了后者：

1. `daemon/index.ts:977 sanitizeForLog` —— 只脱敏 delta 三类。
   于是 `rawArgs`（**累积**的参数全文，`session-host.ts:1600` `track.rawArgs += inner.delta`
   后逐帧 emit）**原样落盘**。累积文本每帧重发全文 → 字节量是 O(n²)。
   这是 `logs/events-*.jsonl` 涨到 **136 MB** 的主因（09-11 单日 44 MB、09-15 26.8 MB / 47,239 行）。
2. `diagnostics-view.tsx:813` 的事件过滤 —— 挡了 `tool_progress`，没挡 `tool_stream_progress`。
   页面开着时，一次 write 会触发 3000+ 次 `refresh()` + `refreshLedger()`（重读全量台账）。
3. `stats-view.tsx:211` 同款过滤 —— 同样失效，每次触发 `readUsageStats` 全目录重扫。

**顺带**：`sanitizeForLog` 也不管 `user_message`，实测单条 **81 KB** —— 与
`core/run-ledger.ts` 文件头「消息正文只在会话 JSONL 落一份」的纪律有张力，待确认。

### bug2：两页的「总 tokens / 费用 / 会话数」口径不同，且无任何提示

实测两目录覆盖面差 4 倍：会话 JSONL **49 个 / 09-08 起（17.4 MB）**；
运行台账 **11 个 / 09-14 起（324 KB）**。诊断页读台账、统计页读会话 JSONL，
两者都标「全历史累计」却是两个全历史 —— 同一问题两个答案。台账是后加的，
老用户看到的差值只会更大。

### bug3：`TokenTrend` 的 X 轴不是时间轴

`daemon/usage-stats.ts:363` 只把 `tokens > 0` 的日子 push 进 `dailyTokens`；
`stats-view.tsx:158` 用 `i * step` 等距排点。**中间断档的日子被压缩**，
30 天里只有 10 天有数据时，10 个点铺满全宽，看不出断档 —— 与热力图（按真实日期网格）
口径相反。另外 `points.length === 1` 时 `step = 0`，单点 polyline 画不出来。

### bug4：格式化的重复实现（AGENTS.md §4）

`formatTokens` / `formatCost` / `StatCard` 在诊断页与统计页**各写一份**；
时长格式化两份且不同名（诊断页 `formatMs` → "3.6s"，统计页 `formatDuration` → "4s"，
**同值不同显示**）。统计页还把「消息数」喂给 `formatTokens`（>100 万会显示 "1.23M 条消息"）。

### 被实测推翻的既有判断（文档该改，我没动）

`core/event-log.ts` 文件头写「**事件量小（每 run 几十条）**」，以及
不做「日志采样 / 按大小轮转」的 YAGNI 理由；`docs/可观测性清单.md` LOG18 同样标 ⛔。
**实测反驳**：单日 4.7 万条 / 26.8 MB，`logs/` 累计 136 MB 且无上限、无清理、
诊断页也不显示它（OPS1 标 ❌ 属实，但严重度被低估）。
`appendFileSync` 同步写在 2.7 万条/天 的写入量下也值得重新评估。

### 其他（次要）

- `estimateComposition` 用 `summary + detail` 估工具结果占用，真实入模的是 toolResult
  全文 —— 「上下文被谁吃了」的核心答案系统性偏低。
- 缓存归因的 `CACHE_TTL_MS = 5min` / 噪声门限 1024 硬编码，不对齐 pi settings。
- `missedCost` 注释说「偏保守」，实际 readPerToken=0 时**高估**（没减掉本可省的读价）。
- `buildHeatmapWeeks` 的 `width` 入参从未被传入（恒 48 列），设计成自适应但没接线。
- 诊断页职责过载：WorkBuddy 是 `/cost` `/context` `/stats` `/diagnostics` 四条命令，
  我们合并成一页 + 统计页；且「最近任务」与「会话时间线」是两套泳道（口径也不同：
  实时 toolSpans vs 台账 fold）。
- 观测与对话割裂：聊天页没有跳到诊断页的入口（`openDiagnostics` 只在侧栏 / 关于页）。

**本轮只调研，未改任何代码与文档。**

## 新增：会话级指标条（对齐 dsh 的 StatsLine）

用户给了 dsh（`开源项目/deepseek-harness`）输入框下方那行读数的截图，要求复刻。

**对标源码**（读完别重翻）：
- `packages/client/ui-conversation/src/client/chat/StatsLine.tsx` —— 组件本体
- `packages/session/session-stats/`（`types.ts` 8 字段 + `README.zh.md` 折叠语义）
- `packages/client/ui-conversation/src/client/chat/turn-metrics.ts` —— TTFT/decode 算法
- `packages/client/ui-conversation/src/client/chat/StatsLine.module.css` —— 布局

**dsh 的机制要点**：
- 投影 `sessionStats` 只有 8 个字段：turns / steps / llmMs / toolMs /
  ttftMs / ttftSteps / decodeMs / decodeTokens。**没有 cost / cacheHit / 输入输出分项** ——
  那些来自另一个投影 `tokenUsage`，由客户端算（`cacheHitPercent` / `billedInputTokens`）。
- 算法：`ttftMs = firstTokenTime − stepStartTime`；
  `decodeMs = completedTime − firstTokenTime`；`tok/s = decodeTokens / (decodeMs/1000)`，
  **只统计「ttftMs 与 output 兼备」的步**（缺任一项整对不计，否则速度被拉低）。
- 「首 token 平均」= `ttftMs / ttftSteps`。
- 分组顺序：`轮·步 | LLM·工具 | 首token平均·tok/s | 缓存命中 | 输入·输出`；
  组内 `" · "`、组间 `" | "`；**空组整组消失**，全空则整行 null。
- **CSS 坑**：`.root` 必须 `display: block` + nowrap + ellipsis。dsh 原注释
  "Block, not flex: text-overflow only elides a block's inline content" ——
  用 flex 会从中间截掉半个字而不是以 … 收尾。
- 上下文占用**故意不在这行**（dsh 注释：one home per fact，它归 composer 的圆环）。
- TTFT 挂在**每步**上，所以流式期间数字会增长；`session_state` 不携带统计。

**我们的落法（零新增 IPC）**：扩展 `core/observability.ts` 的会话级 fold，
新增事件 `session_stats`（照 `context_usage` 的现成模式：daemon 主动推一条），
折进 `conversation.sessionStats`，组件 `renderer/session-stats-line.tsx` 只做展示。
推送时机两处：台账 fold 钩子里的 `llm_call` / `run_end`（避开 tool_call 的高频，
一个 run 几十条），以及 `session_state` 到达时补推（否则切回有历史的会话要等到
下一轮才有数字）。挂载点在 chat-view 的 `<Composer>` 之后（问卷浮层分支外）。

**顺带修的真 bug（口径）**：`cacheHitRate` 分母原是 `input + cacheRead`，**漏了 cacheWrite**。
pi 源码证明三桶互不相交：`anthropic-messages.ts` 里 `usage.input = input_tokens`，
cacheRead/cacheWrite 各自取自 `cache_read_input_tokens` / `cache_creation_input_tokens`
（同文件对 reasoning ⊂ output、cacheWrite1h ⊂ cacheWrite 都标了子集关系，`input` 没有）。
dsh 的 `billedInputTokens` 也是三桶相加。**修正后诊断页 / 统计页的命中率会略降** —— 那是修正。

**命名陷阱（记牢）**：我们的 `turns` = 模型调用次数 = dsh 的 `steps`；
我们的 `runs` = 用户发几轮 = dsh 的 `turns`。读 dsh 文档时别按字面套。

**`formatTokenCount` 补了 M 档**：会话级输入动辄过百万（agent loop 每步重发整个上下文），
K 档会写出「1500.0K」这种读不出量级的数。

**改动面**（5 新 / 6 改）：新 `shared/observability.test.ts`、`renderer/session-stats-line.tsx`、
`renderer/session-stats-line.test.ts`；改 `shared/observability.ts`（SessionStatCard 四字段 +
billedInputTokens + averageTtftMs + decodeTokensPerSecond + 修 cacheHitRate）、
`shared/context-usage.ts`（M 档）、`shared/session-events.ts`（事件 + SessionSnapshot）、
`shared/conversation.ts`（折叠 + 换会话清空）、`core/observability.ts`（累加 + sessionCard）、
`daemon/index.ts`（两处推送）、`renderer/chat-view.tsx` + `index.css`（挂载与样式）。

**验证**：`tsc --noEmit` ✓ ｜ `check:deps` ✓（265 文件）｜ `check:tokens` ✓（真违例 0）｜
全量测试 **99 文件 / 1799 用例全绿**。

**没做**：真机验证（要走 `npm run dev` 手工看）。**改动了 daemon 与 shared，HMR 覆盖不到，
必须重启 dev 进程**。

**清单同步**（`docs/可观测性清单.md`）：计数 82→83（✅ 49→52、❌ 18→16）；
LOG14 / PERF6 ❌→✅（tok/s 落地）；CACHE1 / CACHE2 分母改为三桶之和（billedInputTokens）；
LOG4 补会话级累加与取平均；VIEW7 更名「单轮读数（消息行内）」并写明只 fold 当前轮；
新增 VIEW14「会话级常驻指标条」；优先级建议第 4 条勾销。

**仍未改（属决策重审，等用户拍板）**：LOG18 的 ⛔ 理由「事件量小（每 run 几十条）」
已被实测推翻（单日 4.7 万条 / 26.8MB，logs 累计 136MB 且无轮转、无清理、无界面显示）；
OPS1「观测量自身的健康」的严重度也偏低。

## 修 `tool_stream_progress` 三处漏网 + LOG18 改判（用户确认后动手）

三处都是「名字只差一个 stream」的笔误 —— 名单里写了 `tool_progress`（工具 stdout），
漏了 `tool_stream_progress`（参数生成期逐 delta 进度）：

1. `daemon/index.ts` 的 `sanitizeForLog` —— 加分支，把**累积**的 `rawArgs` 收成
   `(N chars)`（`path` / `added` / `changeType` 是有信息量的小字段，照留）
2. `renderer/diagnostics-view.tsx` 的事件过滤 —— 加进跳过名单
   （此前页面开着时，一次 write 会触发 3000+ 次「重拉快照 + 重读全量台账」）
3. `renderer/stats-view.tsx` 同款（它还要重扫整个会话目录）

**实测量化（重要，别再用「每 run 几十条」估）**：09-15 单日 `tool_stream_progress`
**53,507 条 / 29.5 MB**（带 `rawArgs` 的 4,614 条占其中 13.2 MB）；
**只脱敏 rawArgs → 14.3 MB（降 52%）**，剩下 14 MB 全是「条数」本身。
当日文件 37.8 MB → 修正后约 **22.6 MB**。
→ **体积主因是条数，不是单条大小**：脱敏只解决一半，另一半要靠节流。

**LOG18 从 ⛔ 改判 ❌**（`docs/可观测性清单.md` 与 `core/event-log.ts` 文件头同步更正）：
原 YAGNI 理由不成立，方向定为「按时间窗合并中间态」（保住首末与转折），
不是照抄 WorkBuddy 的事后指纹采样。清单计数 → 83 条：
**✅52 / 🟡10 / ❌17 / ⛔3 / ❓1**。OPS1 严重度同时上调。

**仍未做**：节流本身 —— 属新功能且有多种做法（时间窗合并 / 丢弃中间态 / 阈值触发），
留给用户定。

## 信息架构重排 + 右侧「任务诊断」面板（第一批）

**用户决策（记住，别再反复问）**：
- 诊断页 7 块里只有「应用状态」名副其实 —— 4 块是统计（跨会话宏观）、
  2 块是任务诊断。按粒度分派：统计类 → 统计页；任务级 → 右侧栏；环境类留诊断页。
- **跨会话统计统一到台账口径，放弃「覆盖全历史」**（用户：「过去的数据源不用了也行，
  项目还在开发中」）—— 这会顺带消掉「两个总 tokens 数字对不上」，**第三批落地**。
- 「最近任务」保留但改成跳转（切到那个会话并开侧栏）。
- 右侧栏**跟随会话**（切会话不关，只换数据源）。

**dsh 的对标物是 `packages/client/ui-trajectory`**（不是我之前以为的某个小组件）：
turn-aware event ledger + 本地 inspector，容器挂 `conversation.composer.dock`。
它的 inspector 只有两类字段（Tokens 含 Reasoning/Content 拆分、Request Timing 含
TTFT/Generation/Throughput）—— **上下文 / 工具调用 / 缓存命中是我们多的**，
而这三样数据全在盘上，只是没地方显示。

**本批实现**（面板位第三态，接入成本极低 —— 右侧面板位本来就是 ArtifactPanel /
SourcesPanel 同位互斥复用的）：
- 新建 `renderer/task-diagnostics-panel.tsx`：① 会话总览（读
  `conversation.sessionStats`）+ ③ 轮/步台账（`runLedger(sessionId)` →
  `foldRunLedger` → 平铺列表，轮边界 + 条目前置缩进）。
- App.tsx：`taskDiagOpen` **独立于 `panelOpen`**（前者跟随会话、不参与
  `closePreviewPanel` 的清理；后者随会话关闭）；优先级 诊断 > 来源 > 产物。
- 右上角并排两个按钮；点诊断会 `setSourcesOpen(false)`（否则关掉诊断会冒出 Sources）。

**顺带抽出的三处「唯一处」**（都直接相关，不是顺手重构）：
- `shared/session-events.ts` 的 `isStreamingEvent`（类型守卫）—— 那份「流式 / 进度类
  事件名单」此前在 daemon 与两个页面各写一遍且都漏了 `tool_stream_progress`；
  现在第四个消费方（新面板）出现，不抽就要写第四遍。daemon 的 `sanitizeForLog`
  也改用它（字段处理各按类型：三种 delta 收 `delta`，`tool_stream_progress` 收 `rawArgs`）。
- `renderer/reading-format.ts`：`formatSpan` / `formatThroughput` / `formatCost`
  （指标条与新面板并列消费，放任一组件里都会让另一个反向 import 组件）。
- `shared/observability.ts` 的 `stepDecode`：单步解码窗口与其中的 output token，
  daemon 的累加与侧栏的单步显示共用 —— 两处各算一遍必然漂移，而 tok/s 的分子
  分母最经不起这个。

**未做（第二 / 三批）**：② 上下文占用区（精确 used/total + 估算成分）、
④ 单步详情的展开（该步工具列表 / 入模拆分 / `reasoning` 拆分）、统计页接收 3 块、
诊断页瘦身 + 新增「观测健康」（正好填掉 OPS1）。

## 修：右上角开关按钮重叠（我这批引入的 bug）

现象（用户截图）：右上角图标看着怪、**任务诊断入口根本找不到** ——
而底部指标条正常显示，说明代码整体是生效的。

根因：`panel-toggle-btn` 是 `position: absolute; top: 8px; right: 12px`
（自己钉在右上角，见 index.css）。我在它旁边加第二个按钮时**沿用了同一个类**
→ 两个按钮落在同一坐标，完全重叠，只看得见叠在一起的一个。

修法：**定位上移到容器** —— 新增 `.panel-toggle-group`（absolute + flex + gap），
两个按钮退化成组内横排子项；`.panel-toggle-btn` 只保留 `.active` 态
（它现在只是「active 钩子 + 语义标签」，不再承担定位）。
连带两处：
- `.preview-head` 的右让位 44px → **80px**（组宽 60 + right 12 + 4 余量），
  否则面板 header 行尾的「全屏 / 外部打开」会被加宽的按钮组盖住。
- **删掉** allowlist 里那条 44px 的让位偏移条目：80 已落进「≥48px 页面留白」的
  内置豁免区间，留着会报「白名单条目未命中」。
- `.app` 与 `.sidebar-toggle-btn` 的注释同步（它们写着「按钮 absolute 钉在这里」）。

**教训（值得记）**：给 absolute 定位的控件加兄弟节点前，先确认定位是在**它自己**
身上还是在容器上 —— 沿用同名类会静默完全重叠，而 typecheck 与 token 校验都查不出来。
反过来说，这也是「界面改动必须真机看」的又一个例子。

## 再修右上角：让位算式算漏了一个按钮（用户第二轮截图）

上一版把定位从按钮移到容器（修掉完全重叠），但**还剩三处不协调**：

1. **「还是重叠」的真因 —— 让位算错**：`.chat-header` 的 `padding-right: 48px`
   是给**一个**开关算出来的（12 + 28 + 8）。我的按钮组宽 64px（两个 28 + gap），
   从 right 12 伸到 76px → **行尾的 ModeSwitch（创作）被第二个按钮压住**。
   → `.chat-header` 与 `.preview-head` 的右让位**统一改成 84px**
   （= right 12 + 按钮 28×2 + 组内 gap 8 + 呼吸 8）。两处必须同值，
   否则面板开合时行尾会错位。
2. **组内间距与 header 不同值**：我用 `--space-1`（4px），header 用 `--space-3`（8px）
   → 两块并排时疏密不匀。改成 `--space-3`。
3. **图标语义重复**：原本用 `IconChart`，而那是侧栏「统计」入口的图标。
   → 新增 `IconActivity`（活动脉冲 / 心电图），语义贴「这一轮跑得顺不顺」。

**方法论（下次别再犯）**：absolute 覆盖层的「让位内边距」是个**跟着控件宽度走的算式**，
加减成员时最容易漏 —— 这次 `.chat-header` 与 `.preview-head` 两处都漏了。
动控件的数量/尺寸时，**grep 它的类名把所有「让位偏移」注释一起改**，别只改看得见的那处。
这类错误 typecheck / deps / tokens / 单测**四道校验全查不出来**。

**✅ 真机验证通过（用户 2026-09-15 晚确认「正常了」）**：会话级指标条、任务诊断面板
（右上角脉冲图标）、右上角按钮组排布三件事全部生效。至此这批 UI 改动的
「未做真机验证」风险已关闭；`84px` 让位 / `--space-3` 组内间距 / `IconActivity`
三个常量即为实测确认值，后续勿凭感觉回改。
