# Checklist

## 权限快捷切换

- [x] 首页「默认权限 ▾」chip 点击展开弹层（不再是 toast「待做」），列出「只读 / 默认权限 / 允许完全访问」三项（label + description），当前项有选中标记
- [x] 选「只读」后 chip 立即显示「只读」；随后让 AI 写文件被权限门拒绝且提示切换预设
- [x] 选「允许完全访问」后写工作区外文件不再询问，但读 ~/.ssh 仍被拒、命令仍被拦（既有权限语义不回退）
- [x] 首页切换后打开设置页，权限区显示一致（同一数据源）
- [x] 弹层底部「打开设置」可跳转设置页
- [x] 切到与预设不符的旋钮组合（设置页单改旋钮）后 chip 显示「自定义」，弹层无选中项

## 会话列表

- [x] 应用启动后侧栏「任务」区列出全部历史会话，按修改时间倒序，当前会话高亮且至多一条
- [x] 每项显示标题（命名的显示命名，未命名的显示首条消息截断）+ 智能时间戳 + 空间标识（playground 显示「不使用工作空间」）
- [x] 新建任务后旧会话保留在列表中；当前空会话（未发消息）不出现在列表

## 会话恢复

- [x] 点击历史会话 → 对话页显示完整历史（用户消息、助手回复、思考块、工具卡片状态正确），并自动切到对话页
- [x] 恢复后继续提问，模型记得之前的内容（上下文连续）
- [x] 恢复 playground 会话后，工作空间状态恢复为「不使用工作空间」，预览面板引导文案正常
- [x] 恢复正式空间会话后，工作空间选择同步恢复，预览服务根同步
- [x] 恢复不改变当前模型与权限设置
- [x] 流式进行中点恢复被拒并提示
- [x] 越界路径（如 `../auth.json`）resume 被 daemon 拒绝且当前会话不受影响
- [x] 关闭应用重开，历史会话仍能找回并继续（T4 验收标准）

## 重命名与删除

- [x] 行内重命名：Enter 提交立即生效，Esc 取消；重启应用后命名仍在
- [x] 删除有行内二次确认；删除后列表消失，文件出现在 ~/.kamibuddy/trash/（同名有时间戳前缀）
- [x] 当前活动会话删除被拒并提示先新建任务

## 工程验证

- [x] `npm run check`（typecheck + check:deps）通过
- [x] `npm test` 全绿（含 session-rebuild 新增 ≥10 用例）
- [x] `npm run smoke:session` 13/13 不回退
- [x] docs/STATUS.md 已更新（T4 落地 + 权限入口修正 + 待用户验证事项），docs/ROADMAP.md T4 勾选
