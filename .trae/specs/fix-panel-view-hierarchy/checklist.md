# Checklist

- [x] 头部下拉是视图切换器：按钮显示当前视图名，菜单为 概览/工作空间文件/变更 三个平级项，当前项带 ✓（不再内嵌文件列表）（ViewSwitcher）
- [x] 概览视图只含产物折叠组（计数、空态「暂无内容」），产物条目行为不变（单击预览/双击转正/URL 外部打开/大小显示）（OverviewView 逐条沿用）
- [x] 变更视图顶部有「文件变更 +N -M」汇总头（合计口径），变更条目带 +/- 徽章，点击进 diff 预览行为不变（ChangesView + .changes-summary）
- [x] 工作空间文件视图显示目录树（切到该视图时拉取，懒展开/转圈语义不变；无工作区占位沿用）（WorkspaceView）
- [x] 主体双态正确：点条目进预览态；预览态切视图回列表态且 tabs 保留可点回；关最后一个 tab 自动回列表态（panel-view.ts 状态机 11 个迁移测试，含外部打开/会话切换的同步 effect）
- [x] 旧「概览下拉内嵌三组」结构无残留（grep 确认 preview-menu-group 三组 JSX 已删，仅剩注释引用）
- [x] `npm run typecheck && npm run check:deps && npm test` 全绿（653 个测试，独立复跑确认）
- [ ] 冒烟（用户重启后）：三视图切换、产物组、变更汇总计数、目录树、双态往返均正常
