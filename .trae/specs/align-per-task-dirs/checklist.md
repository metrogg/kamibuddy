# Checklist

## 自动目录

- [x] 新任务在首次执行时获得 `<生效根>/YYYY-MM-DD-HH-mm-ss`（本地时间）目录，会话 cwd 指向它
- [x] 同一秒冲突按秒递增重试（有单测：预建同名目录后仍能分配）
- [x] 点了「新建任务」但未发消息时不产生空目录（不建目录）
- [x] 两个任务的同名产物分别落在各自目录，不互相覆盖（有测试或可复现步骤）

## 归属判定

- [x] 任务区判定不依赖当前生效根：改存储根后旧任务仍在「任务」区，不漂到「空间」区
- [x] 自动目录（时间戳）、历史共享临时目录、生效根本身 → 任务区
- [x] 已被显示名覆盖的目录 → 空间区（有测试）
- [x] 既有「空间」区按 cwd 分组、组内排序、折叠等行为无回归

> **【2026-09-15 订正】** 本组第二条后半「生效根本身 → 任务区」已作废：cwd = 生效根本身
> 现归**空间区**成组（对齐 WorkBuddy；`isTaskCwd` 已移除该判定）。自动目录、历史共享临时
> 目录仍归任务区，其余条目不变。

## 转正

- [x] 「保存到工作空间」把任务目录重命名为空间名（产物与记忆随目录迁移，无残留）
- [x] rename 失败（目录被占用）时响亮报错，会话 cwd 与目录保持原样
- [x] 目标名与根下既有目录重名时拒绝，不产生半成品状态
- [x] 转正后该任务归「空间」区、与同名目录同组

## 入口与口径

- [x] 任务行菜单有「打开文件夹」，且能打开时间戳目录（daemon 校验已放宽到会话 cwd）
- [x] 未知路径仍被 reveal 校验拒绝（放宽不等于放开）
- [x] 导出目录与技能资源加载改用生效根（设置里改「默认存储路径」后随之变化）
- [x] 历史 `<根>/临时任务` 目录里的既有会话仍归任务区，文件原地保留未被删除

## 工程

- [x] 命名/判定契约只在 `shared/workspace.ts` 一份实现，daemon 与 renderer 共用（无重复逻辑）
- [x] `npm run typecheck && npm run check:deps && npm test` 全绿
- [x] 对齐清单已更新，且旧 spec 的「Claw = 临时任务共享目录」表述已更正
