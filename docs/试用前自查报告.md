# 试用前自查报告（2026-09-17）

> 面向部门宣讲试用的全面自查：自动化体检 + 深度风险排查（通道接线 / 半成品 / 覆盖缺口 / 高危边）+ 手动测试清单。
> 结论先行：**核心链路健康**（2200+ 项单测 + 冒烟全绿，仅 3 个既有 worktree 失败）。自查发现的 P0（MCP 工具模型不可见）与 P1（死入口/占位按钮）**均已当日修复并验证**；剩余事项为环境准备与试用说明。

## 一、自动化体检结果

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| `npm run check`（类型/依赖规则/设计 token） | ✅ | 全过 |
| 全量单测 | ✅ 2212 过 / 3 败 / 1 skip | 3 个失败全是 `src/core/worktree.test.ts` 副本清理时序（既有，非新增） |
| `npm run build`（生产构建） | ✅ | 编译通过 |
| smoke:sdk / session / session-branch / permission / sandbox | ✅ 3/3, 14/14, 31/31, 10/10, 9/9 | 会话分支冒烟新增「重试不抽枝」F1-F3 用例，全过 |
| **smoke:mcp-activation（新增）** | ✅ 3/3 | 真实 pi 会话 + 本地 stdio echo server（零网络），断言 MCP 工具**模型侧可见** |
| smoke:mcp | ⚠️ 本机失败 | 环境问题：npm registry PING 50s，npx 首拉包过不了 30s 握手。需在联网正常的环境复跑 |
| smoke:docx / docx-roundtrip | ⚠️ 本机失败 | 环境问题：缺 `uv`（docx 引擎的 Python 管理器）。**演示机必须预装并跑通一次** |
| 会话分支修复（重试不抽枝 + 回答条分支按钮） | ✅ | smoke 31/31 + tsc + renderer 测试全过 |

## 二、问题清单（按优先级）

### P0 — 功能性缺陷 ✅ 已修复（2026-09-17）

**MCP 工具注册了，但模型永远调不到。**
- 链条：mcp-client 把工具注册成 `mcp__<server>__<tool>` → pi 的 `createAgentSession` 把 `tools` 数组当作**激活名单**（pi sdk.js:141 `options.tools → allowedToolNames`），且 `_refreshToolRegistry` 对扩展工具**先按名单过滤再放行进注册表**（agent-session.js:2117）→ 三个模式白名单（resources/modes/*.md）都不含 `mcp__*` → 工具连注册表都进不去，事后 `setActiveToolsByName` 无效。
- 症状：连接器页显示「已连接，N 个工具」，模型实际一个都调不到。
- 为什么测试没拦住：smoke:mcp 用假 ExtensionAPI 验证注册，从未端到端验证「模型侧可见」。
- **修复**（对齐 WorkBuddy 的「MCP 一等公民」口径——同名 `mcp__server__tool` 进名单与权限规则）：session-host 新增 `extraActiveTools` 选项，MCP 工具名在**构造时**并入 `tools`（扩展工厂在 `resourceLoader.reload()` 里同步跑完，名字此刻已就绪）；daemon 传 `mcpClient.registeredToolNames()`。MCP 工具调用仍由权限门逐次审批（permission-policy 的 MCP 分支，凭据拦截/只读拒/审批语义不变）。
- **验证**：新增 `smoke:mcp-activation`（真实 pi 会话 + 本地 stdio echo server，零网络可跑），断言激活集同时含模式白名单成员与 MCP 工具，3/3 通过。
- 已知边界：会话**中途**新配置的 server（名单快照之外）其工具要下个会话才可见；名单内 server 的启停/重连不受影响。

### P1 — 演示观感 ✅ 已修复（2026-09-17）

1. **输入区「+」菜单的「技能」「连接器」死入口** → 已接线：PlusMenu 新增 `onOpenSkills` / `onOpenConnectors`，经 App 的 `openSkillsAt` 跳技能页对应页签（SkillsView 新增 `initialTab`）；首页与对话页的「+」菜单都已接。
2. **「语音输入」占位按钮**（首页 + 对话页）→ 已移除：可见按钮点了只 toast「待做」，观感比没有更差；能力落地时再放回（原实现见 git 历史）。
3. **专家页「专家团」子 tab**：EmptyState 占位（experts-view.tsx:213）。低危，保留。

### P2 — 体验边界（试用说明里写明，或列入修复排期）

4. **在途审批/问卷在 renderer 重载后永久悬挂**：daemon 的 pendingApprovals 不进 snapshot 也无超时（daemon/index.ts:1410），HMR/手动刷新/渲染崩溃重载后，工具的 await 永不 resolve，任务卡「正在思考」，只能点停止。修复方向：pending 请求并入 snapshot（或挂载时补推）。
5. **daemon 挂 = 应用死**：main 不自动重启（有意，main/index.ts:119），断开提示只有侧栏一行字，无「请重启」全屏出口；main 也无 `render-process-gone` 自动 reload。
6. **项目信任询问永远不弹**：ui:request 通道 daemon 侧故意 throw（预留），`ctx.hasUI` 恒 false（project-trust.ts:51），非自家目录的 `.pi/extensions` 静默跳过——行为 fail-closed 安全，但「问了才信任」未达成。
7. **Electron 重启后会话不自动恢复、错过的定时任务不补跑**（automation-scheduler.ts:63 有意设计）——写进试用说明，避免被当 bug 报。
8. **死通道**：`saveArtifactAs`（main 完整实现、renderer 零调用）；`ui:response` 预留。低危，可清理或留着。

### P3 — 测试覆盖缺口（不阻塞试用）

- runner 层三个文件零直测：`automation-runner.ts`（273 行）、`subagent-runner.ts`（407 行）、`member-runner.ts`（191 行）。
- `skills-cost.ts` 无测（仅阈值文案）。
- daemon/index.ts 的 restartSession/forkSession 接线只有 smoke 探针覆盖（已补「重试不抽枝」用例）。
- `src/core/worktree.test.ts` 3 个既有失败该修（副本清理时序）。

## 三、分发与环境准备（宣讲前必须落实）

**当前没有打包方案**（README 明说「还不能双击安装」）。给同事试用 = 每台机器：

1. Node ≥ 22.19 + git
2. `git clone` + `npm install`——**内网 npm 慢是实测风险**（本机 registry PING 50s），提前配内网镜像或离线 npm 缓存
3. 首次 `npm run dev` 启动
4. **docx 生成**：装 `uv`（或预置 venv 指 `HTML_TO_DOCX_VENV`）；首转 ensure 需联网 1-3 分钟
5. **MCP 连接器**：首次连接要 npx 拉包，内网慢会握手超时转 failed（`smoke:mcp-activation` 的本地 echo server 不受影响）
6. 模型凭据：统一用公司中���（jlc）的 baseUrl + Key + 「鉴权走 Authorization 头」勾选

## 四、手动测试清单（宣讲前过一遍）

### A. 核心会话（最高频路径）
- [ ] 新建任务 → 发消息 → 流式回复 → 追问 → 中途停止
- [ ] 权限弹窗：允许 / 拒绝 / 总是允许（rememberPrefix）三态各验一次；拒绝后模型收到原因且不重试同一路径
- [ ] 权限模式切换（只读/默认/完全访问），默认档下名单外命令直接进沙箱跑、写盘被拦出提示
- [ ] 长会话触发压缩（/compact 手动 + 自动），压缩后继续对话上下文连贯
- [ ] 错误恢复：改一个错误 Key 触发报错 → 错误卡出现 → 修 Key → 重试成功

### B. 会话管理
- [ ] 回答操作条：复制 / 分支 / 重试——**重试后侧栏不得新增会话**（2026-09-17 修复项）
- [ ] 用户气泡 hover：重新开始（产生分支存档）/ 分支出新会话（母会话不动、自动切换、原文回填）
- [ ] 侧栏：重命名、归档、删除、导出 HTML；分支会话带「· 分支」标记
- [ ] 切换会话再切回，历史与折叠状态保持

### C. 文档能力
- [ ] 拖入 docx / xlsx / pptx / pdf 提问，读取正常
- [ ] HTML→docx 生成（演示机先装 uv！），生成文件能打开
- [ ] 产物面板预览 / 下载

### D. 技能 / 连接器 / 自动化
- [ ] 输入区「+」菜单 → 技能 / 连接器：直达对应页签（2026-09-17 接线）
- [ ] 技能安装、`/skill` 调用、启停
- [ ] MCP：配置 server → 连接 → **让模型真的调用一次工具**（P0 修复后应可行；配置后新开会话）
- [ ] 自动化：创建定时任务 → 触发 → 结果会话出现；重启后不补跑（向试用者说明）

### E. 跨机器 / 环境差异
- [ ] **非管理员账号**下跑一次（开发会话是提权环境，沙箱在标准权限下未被实测过）
- [ ] 公司网络下模型连通（jlc 中转）、npm/MCP 慢网表现
- [ ] 动效在系统动画关闭的机器上照常（已改为无条件呈现，验证一次）
- [ ] 杀毒软件不拦沙箱子进程（restricted token + Job Object）

### F. 异常与恢复
- [ ] 手动刷新页面（F5）：在途审批会悬挂（P2-4 已知）——刷新前确认无任务在跑
- [ ] 停止键在流式 / 工具执行 / 压缩中各按一次，均能停下
- [ ] 断网发消息 → 错误卡 → 恢复网络 → 重试
